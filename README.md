# Thndr Nasdaq Stocks

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

## How To Run

Create .env file and add the api key from Tickers e.g: `VITE_API_KEY=abcde`

1-Run `yarn` to install dependancies

2-Run `yarn dev` to run the project
